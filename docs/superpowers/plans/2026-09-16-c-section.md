# C 段 17–28 小节

按用户已确认的独立 HTML 方案追加 C 段，保持已审查的 1–16 小节、每行四小节、谱前图例及延音/装饰音说明独立一行。

1. 逐小节转录并独立复核弦品、时值、连奏、延音、三连音、星号；红框外内容同样保留。
2. 新增结构化 C 段数据及三连音、文本标记的 AlphaTex 输出；星号含义已由用户确认为打板。
3. 合并至第 28 小节，更新范围及核对说明，保持单文件离线与旧入口同步。
4. 验证每小节四拍、解析后技法关系与原数据一致；检查七行布局、离线、缩放、定位、窄屏与分页打印。

小节信息将 C 段指定为 17–28，D 段指定为 26–36；本次按 C 段完整转录到 28，后续 D 段不得重复插入 26–28。
