# 前奏 HTML Implementation Plan

**Goal:** 生成供用户审查的第 1–4 小节独立 HTML 六线谱。

**Architecture:** 结构化转录存入 score/intro.json；scripts/build.mjs 将其转换为 alphaTex 并内嵌 alphaTab、Bravura 字体及页面。输出 前奏.html。

**Tech Stack:** Node.js、alphaTab 1.8.4、HTML/CSS、Playwright。

- [x] 核对四张原图：弦位、时值及连线。记录截图不确定处。
- [x] 建立 score/intro.json，包括时值、弦品、演奏技巧与来源说明。
- [x] 编写 tests/score.test.mjs，验证四小节拍数、弦品范围、alphaTab 解析后的音符与关键技巧。
- [x] 编写 scripts/build.mjs 与 src/template.html，将脚本和字体内嵌。
- [x] 运行 npm run build 与 npm test，检查四小节均被解析，无隐式补拍。
- [x] 使用 Playwright 通过 file URL 打开产物，断网检查谱面 SVG、浏览器错误、缩放、打印布局以及窄屏。
- [x] 查看渲染截图并对照原图，交付前奏 HTML；后续小节等待用户审查。

当前目录无 Git 仓库，因此不创建分支或提交。独立文件直接写入用户指定的项目。

验证结果：npm test 2/2 通过；离线浏览器检查通过（零 HTTP 请求、零页面脚本错误）；打印 PDF 为单页 A4 横向。已逐图复核弦品、节奏与技巧，另经独立审阅未发现转录错误。
